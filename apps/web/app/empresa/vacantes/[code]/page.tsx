"use client";import {useEffect,useState} from "react";import {api} from "../../../../lib/api";export default function Vacante({params}:any){const code=params.code;const [items,setItems]=useState<any[]>([]),[msg,setMsg]=useState("");useEffect(()=>{api("/v1/company/vacancies").then(x=>setItems(x.items)).catch(()=>setMsg("No se pudo cargar la vacante."))},[]);const v=items.find(x=>x.vacancy_code===code);async function payment(e:any){e.preventDefault();const f=new FormData(e.currentTarget);setMsg("Enviando referencia…");try{await api("/v1/company/vacancies/"+code+"/payment",{method:"POST",body:JSON.stringify({reference:f.get("reference"),notes:f.get("notes")})});setMsg("Pago enviado para revisión.")}catch{setMsg("No se pudo enviar el pago.")}}return <main className="portal"><header><a href="/empresa">Empleos.pa</a><span>{code}</span></header><section><p className="eyebrowDark">VACANTE</p><h1>{v?.position||code}</h1>{v&&<div className="vacancyStatus"><span>Estado actual</span><strong>{v.status}</strong><p>{v.quantity} puesto(s) · {v.work_location}</p></div>}<p className="saveMsg">{msg}</p>{v?.status==="PENDIENTE_PAGO"&&<form className="paymentForm" onSubmit={payment}><h2>Reportar pago</h2><p>Ingresa la referencia del pago. Empleos.pa la revisará antes de iniciar la búsqueda.</p><label className="wfield">Referencia<input name="reference" required/></label><label className="wfield">Observación<textarea name="notes" rows={3}/></label><button className="portalAction" type="submit">Enviar a revisión</button></form>}{v?.status==="PAGO_EN_REVISION"&&<div className="infoBox"><strong>Pago en revisión</strong><p>Te mostraremos el nuevo estado cuando sea aprobado o rechazado.</p></div>}{["APROBADA","EN_BUSQUEDA","EN_CONFIRMACION"].includes(v?.status)&&<div className="infoBox"><strong>Búsqueda en proceso</strong><p>Estamos trabajando con candidatos compatibles y confirmando su interés.</p></div>}{v?.status==="ENTREGADA"&&<div className="infoBox"><strong>Preselección entregada</strong><p>La entrega fue registrada. La empresa continúa directamente con entrevistas, verificaciones y decisión de contratación.</p></div>}{v?.status==="CERRADA"&&<div className="infoBox"><strong>Proceso cerrado</strong><p>Esta vacante ya completó su ciclo en Empleos.pa.</p></div>}</section></main>}
