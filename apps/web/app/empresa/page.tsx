"use client";import {useEffect,useState} from "react";import {api} from "../../lib/api";import {ProfileSwitch} from "../../components/ProfileSwitch";import {LogoutButton} from "../../components/LogoutButton";export default function Empresa(){const [company,setCompany]=useState<any>(null),[needsProfile,setNeedsProfile]=useState(false),[items,setItems]=useState<any[]>([]);async function disableAccount(){if(!confirm("Esto desactivará tu cuenta y cancelará las solicitudes que todavía estén abiertas. Los pagos, entregas y registros históricos ya realizados se conservarán. ¿Seguro que deseas desactivar tu cuenta?"))return;try{await api("/v1/company/account/disable",{method:"POST",body:JSON.stringify({confirm:"DESACTIVAR"})});location.href="/"}catch{alert("No se pudo desactivar la cuenta.")}}useEffect(()=>{api("/v1/company/profile").then(x=>setCompany(x.company)).catch((e:any)=>{if(e?.status===404)setNeedsProfile(true)});api("/v1/company/vacancies").then(x=>setItems(x.items)).catch(()=>{})},[]);return <main className="portal"><header><strong>Empleos.pa</strong><div className="headerActions"><span>Portal de empresa</span><LogoutButton/></div></header><section><p className="eyebrowDark">BUSCA TALENTO</p><h1>{company?company.name:"Registra tu primera solicitud."}</h1><p>Solicita personal para una vacante o encuentra perfiles para servicios profesionales, técnicos y trabajos eventuales. Sigue cada búsqueda desde aquí.</p>{needsProfile&&<div className="infoBox"><strong>Completa los datos de tu empresa</strong><p>Antes de publicar tu primera solicitud necesitamos los datos básicos de contacto y ubicación.</p><a className="portalAction" href="/empresa/perfil">Completar datos</a></div>}<div className="infoBox"><strong>Promoción de lanzamiento</strong><p>Por tiempo de lanzamiento puedes registrar tu solicitud sin pago y pasar directamente a la búsqueda de perfiles compatibles.</p></div><div className="portalActions"><a className="portalAction" href={needsProfile?"/empresa/perfil":"/empresa/vacantes/nueva?solicitud=vacante"}>Nueva vacante</a><a className="portalAction secondaryPortal" href={needsProfile?"/empresa/perfil":"/empresa/vacantes/nueva?solicitud=eventual"}>Servicios y trabajos eventuales</a></div>{items.length>0&&<><h2 className="portalSub">Mis solicitudes</h2><div className="companyVacancies">{items.map(x=><a key={x.vacancy_code} href={"/empresa/vacantes/"+x.vacancy_code}><div><strong>{x.vacancy_code} · {x.position}</strong><p>{x.request_type==="EVENTUAL"?"Servicio o trabajo eventual":`${x.quantity} puesto(s)`} · {x.work_location}</p></div><b>{x.status}</b></a>)}</div></>}<div className="panelProfileSwitch"><ProfileSwitch current="EMPRESA"/></div><div className="dangerZone"><strong>Zona de peligro</strong><p>Esta acción desactiva la identidad completa y cancela las solicitudes abiertas. Requiere confirmación.</p><button onClick={disableAccount}>Desactivar mi cuenta</button></div></section></main>}
