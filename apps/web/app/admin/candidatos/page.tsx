"use client";import {useEffect,useState} from "react";import {api} from "../../../lib/api";
export default function Candidatos(){const [items,setItems]=useState<any[]>([]),[status,setStatus]=useState(""),[q,setQ]=useState(""),[msg,setMsg]=useState("");async function load(){setMsg("");try{const params=new URLSearchParams();if(status)params.set("status",status);if(q.trim())params.set("q",q.trim());const x=await api("/v1/admin/candidates"+(params.size?"?"+params.toString():""));setItems(x.items)}catch{setMsg("No se pudieron cargar los candidatos.")}}useEffect(()=>{load()},[status]);return <main className="admin"><header><a href="/admin">Empleos.pa</a><span>Candidatos</span></header><section><p className="eyebrowDark">ADMINISTRACIÓN</p><h1>Candidatos</h1><div className="rowActions"><input value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>{if(e.key==="Enter")load()}} placeholder="Código, nombre, área o provincia"/><select value={status} onChange={e=>setStatus(e.target.value)}><option value="">Todos los estados</option><option>BORRADOR</option><option>ACTIVO</option><option>VENCIDO</option><option>RETIRADO</option></select><button onClick={load}>Buscar</button></div><p className="saveMsg">{msg}</p><div className="adminList">{items.length===0&&!msg?<p>No hay candidatos para este filtro.</p>:items.map(x=><article key={x.candidate_id}><div><strong>{x.candidate_code||"Sin código"} · {x.full_name||"Perfil sin completar"}</strong><p>{x.primary_job_area||"Sin área"} · {[x.province,x.district].filter(Boolean).join(", ")||"Sin ubicación"} · Vigencia: {x.valid_until?String(x.valid_until).slice(0,10):"pendiente"}</p></div><b>{x.status}</b></article>)}</div></section></main>}