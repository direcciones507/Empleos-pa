"use client";import {useEffect,useState} from "react";import {api} from "../../lib/api";export default function Candidato(){const [profile,setProfile]=useState<any>(null),[count,setCount]=useState(0);useEffect(()=>{api("/v1/candidate/profile").then(x=>setProfile(x.profile)).catch(()=>{});api("/v1/candidate/opportunities").then(x=>setCount(x.items.length)).catch(()=>{})},[]);return <main className="portal"><header><strong>Empleos.pa</strong><span>Portal del candidato</span></header><section><p className="eyebrowDark">TU PERFIL LABORAL</p><h1>{profile?.status==="ACTIVO"?"Tu perfil está activo.":"Vamos a construir tu perfil."}</h1>{profile?.candidate_code&&<div className="profileCode"><span>Tu código</span><strong>{profile.candidate_code}</strong><b>{profile.status}</b></div>}<p>{profile?.status==="ACTIVO"?"Mantén tu información actualizada para que las preselecciones sean más precisas.":"Completa tus datos una sola vez. Después podrás actualizar tu información cuando lo necesites."}</p><div className="portalActions"><a className="portalAction" href="/candidato/perfil">{profile?"Actualizar mi perfil":"Completar mi perfil"}</a><a className="portalAction secondaryPortal" href="/candidato/oportunidades">Oportunidades {count>0&&<b>{count}</b>}</a></div></section></main>}