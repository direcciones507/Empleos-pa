"use client";import {useEffect,useState} from "react";import {api} from "../../../lib/api";
export default function Oportunidades(){const [items,setItems]=useState<any[]>([]),[msg,setMsg]=useState("");async function load(){try{const x=await api("/v1/candidate/notifications");setItems(x.items)}catch{setMsg("No pudimos cargar tus avisos.")}}useEffect(()=>{load()},[]);async function read(id:string){try{await api("/v1/candidate/notifications/"+id+"/read",{method:"POST",body:"{}"});await load()}catch{}}return <main className="portal"><header><a href="/candidato">Empleos.pa</a><span>Avisos</span></header><section><p className="eyebrowDark">AVISOS DE BÚSQUEDA</p><h1>Mantente pendiente.</h1><p>Cuando tu perfil sea incluido en una búsqueda y entregado a una empresa, te avisaremos aquí. El aviso no significa entrevista ni contratación.</p><p className="saveMsg" role="alert" aria-live="polite">{msg}</p><div className="opportunities">{items.length===0&&<div className="emptyState"><strong>No tienes avisos todavía.</strong><p>Tu perfil activo seguirá participando en búsquedas compatibles.</p></div>}{items.map(x=><article key={x.notification_id} onClick={()=>!x.read_at&&read(x.notification_id)} onKeyDown={e=>{if(!x.read_at&&(e.key==="Enter"||e.key===" ")){e.preventDefault();read(x.notification_id)}}} role={x.read_at?undefined:"button"} tabIndex={x.read_at?undefined:0}><div className="oppTop"><span>{x.read_at?"VISTO":"NUEVO"}</span><strong>{x.title}</strong></div><p>{x.message}</p><p><b>Fecha:</b> {new Date(x.created_at).toLocaleDateString("es-PA")}</p></article>)}</div></section></main>}