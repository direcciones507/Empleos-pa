"use client";import {useEffect,useState} from "react";import {api} from "../../../lib/api";export default function Oportunidades(){const [items,setItems]=useState<any[]>([]),[msg,setMsg]=useState("");async function load(){try{const x=await api("/v1/candidate/opportunities");setItems(x.items)}catch{setMsg("No pudimos cargar tus oportunidades.")}}useEffect(()=>{load()},[]);async function respond(id:string,interested:boolean){setMsg("Guardando respuesta…");try{await api("/v1/candidate/opportunities/"+id+"/respond",{method:"POST",body:JSON.stringify({interested})});setMsg(interested?"Interés confirmado. Empleos.pa podrá compartir tus datos de contacto para esta vacante.":"Respuesta registrada.");await load()}catch{setMsg("No se pudo registrar tu respuesta.")}}return <main className="portal"><header><a href="/candidato">Empleos.pa</a><span>Oportunidades</span></header><section><p className="eyebrowDark">OPORTUNIDADES</p><h1>¿Te interesa?</h1><p>Revisa cada oportunidad y decide. Tus datos de contacto solo se comparten para esta vacante cuando confirmas que te interesa.</p><p className="saveMsg">{msg}</p><div className="opportunities">{items.length===0&&<div className="emptyState"><strong>No tienes oportunidades pendientes.</strong><p>Cuando una vacante coincida con tu perfil y seas preseleccionado, aparecerá aquí.</p></div>}{items.map(x=><article key={x.vacancy_candidate_id}><div className="oppTop"><span>{x.vacancy_code}</span><strong>{x.position}</strong></div><p><b>Empresa:</b> {x.company_name}</p><p><b>Lugar:</b> {x.work_location}</p><p><b>Horario:</b> {x.schedule}</p>{x.salary&&<p><b>Salario:</b> {x.salary}</p>}<div className="oppActions"><button onClick={()=>respond(x.vacancy_candidate_id,false)}>No me interesa</button><button className="yes" onClick={()=>respond(x.vacancy_candidate_id,true)}>Me interesa</button></div></article>)}</div></section></main>}
