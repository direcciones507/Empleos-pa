import crypto from "node:crypto";
import type {FastifyInstance} from "fastify";
import {db} from "./db.js";
import {normalizeEmail,type Role} from "./auth.js";
const COOKIE="empleos_session";
const digest=(v:string)=>crypto.createHash("sha256").update(v).digest("hex");
const random=()=>crypto.randomBytes(32).toString("base64url");
function settings(){const clientId=process.env.GOOGLE_CLIENT_ID,clientSecret=process.env.GOOGLE_CLIENT_SECRET,redirectUri=process.env.GOOGLE_REDIRECT_URI,webUrl=process.env.WEB_URL;if(!clientId||!clientSecret||!redirectUri||!webUrl)throw new Error("Google OAuth is not configured");return {clientId,clientSecret,redirectUri,webUrl};}
function safeReturn(v:unknown){return typeof v==="string"&&v.startsWith("/")&&!v.startsWith("//")?v:"/";}
function roleReturn(role:Role,requested:unknown){const dest=safeReturn(requested);if(role==="CANDIDATO")return dest.startsWith("/candidato")?dest:"/candidato";if(role==="EMPRESA")return dest.startsWith("/empresa")?dest:"/empresa";return "/admin";}
export async function googleOAuthRoutes(app:FastifyInstance){
app.get("/v1/auth/google/start",async(req:any,reply)=>{const s=settings();const role:Role|undefined=req.query?.role==="CANDIDATO"||req.query?.role==="EMPRESA"?req.query.role:undefined;const state=random();await db.query("insert into oauth_states(state_hash,provider,requested_role,return_to,expires_at) values($1,'google',$2,$3,now()+interval '10 minutes')",[digest(state),role??null,safeReturn(req.query?.returnTo)]);const p=new URLSearchParams({client_id:s.clientId,redirect_uri:s.redirectUri,response_type:"code",scope:"openid email profile",state,prompt:"select_account"});return reply.redirect("https://accounts.google.com/o/oauth2/v2/auth?"+p);});
app.get("/v1/auth/google/callback",async(req:any,reply)=>{const s=settings();const {code,state}=req.query??{};if(typeof code!=="string"||typeof state!=="string")return reply.code(400).send({error:"INVALID_OAUTH_CALLBACK"});const client=await db.connect();try{await client.query("begin");const st=await client.query("select requested_role,return_to from oauth_states where state_hash=$1 and provider='google' and used_at is null and expires_at>now() for update",[digest(state)]);if(!st.rowCount){await client.query("rollback");return reply.code(400).send({error:"OAUTH_STATE_EXPIRED_OR_USED"});}await client.query("update oauth_states set used_at=now() where state_hash=$1",[digest(state)]);const tr=await fetch("https://oauth2.googleapis.com/token",{method:"POST",headers:{"content-type":"application/x-www-form-urlencoded"},body:new URLSearchParams({code,client_id:s.clientId,client_secret:s.clientSecret,redirect_uri:s.redirectUri,grant_type:"authorization_code"})});if(!tr.ok)throw new Error("Google token exchange failed");const tokens:any=await tr.json();const ur=await fetch("https://openidconnect.googleapis.com/v1/userinfo",{headers:{authorization:`Bearer ${tokens.access_token}`}});if(!ur.ok)throw new Error("Google userinfo failed");const profile:any=await ur.json();if(!profile.sub||!profile.email||profile.email_verified!==true){await client.query("rollback");return reply.code(400).send({error:"GOOGLE_EMAIL_NOT_VERIFIED"});}const email=normalizeEmail(profile.email);let u=await client.query("select user_id,email,role,status,google_subject from users where google_subject=$1 or normalized_email=$2 order by (google_subject=$1) desc limit 1",[profile.sub,email]);if(u.rowCount){const x=u.rows[0];if(x.status!=="ACTIVE"){await client.query("rollback");return reply.code(403).send({error:"ACCOUNT_DISABLED"});}if(x.google_subject&&x.google_subject!==profile.sub){await client.query("rollback");return reply.code(409).send({error:"ACCOUNT_CONFLICT"});}if(!x.google_subject)await client.query("update users set google_subject=$1,email_verified_at=coalesce(email_verified_at,now()),updated_at=now() where user_id=$2",[profile.sub,x.user_id]);u=await client.query("select user_id,email,role from users where user_id=$1",[x.user_id]);}else{const role=st.rows[0].requested_role;if(!role){await client.query("rollback");return reply.redirect(s.webUrl+"/registro?oauth=choose-role");}u=await client.query("insert into users(email,normalized_email,google_subject,role,email_verified_at) values($1,$2,$3,$4,now()) returning user_id,email,role",[profile.email,email,profile.sub,role]);}const raw=random();await client.query("insert into auth_sessions(user_id,token_hash,expires_at) values($1,$2,now()+interval '14 days')",[u.rows[0].user_id,digest(raw)]);await client.query("commit");reply.setCookie(COOKIE,raw,{httpOnly:true,sameSite:"lax",secure:process.env.NODE_ENV==="production",path:"/",maxAge:60*60*24*14});const dest=roleReturn(u.rows[0].role,st.rows[0].return_to);return reply.redirect(s.webUrl+dest);}catch(e){await client.query("rollback").catch(()=>{});throw e;}finally{client.release();}});
}
