function required(name:string){const value=process.env[name];if(!value)throw new Error(`Missing required environment variable: ${name}`);return value;}
export const config={port:Number(process.env.PORT??3001),host:process.env.HOST??"0.0.0.0",databaseUrl:required("DATABASE_URL"),nodeEnv:process.env.NODE_ENV??"development",webUrl:process.env.WEB_URL??"http://localhost:3000",candidateValidityDays:Math.max(1,Number(process.env.CANDIDATE_VALIDITY_DAYS??45)),emailWebhookUrl:process.env.EMAIL_WEBHOOK_URL??"",emailWebhookSecret:process.env.EMAIL_WEBHOOK_SECRET??"",emailFrom:process.env.EMAIL_FROM??"Empleos.pa",requestPaymentMode:(process.env.REQUEST_PAYMENT_MODE??"FREE").toUpperCase()==="MANUAL"?"MANUAL":"FREE",deepSeekApiKey:process.env.DEEPSEEK_API_KEY??"",deepSeekBaseUrl:process.env.DEEPSEEK_BASE_URL??"https://api.deepseek.com",deepSeekModel:process.env.DEEPSEEK_MODEL??"deepseek-chat",deepSeekTimeoutMs:Math.min(30000,Math.max(3000,Number(process.env.DEEPSEEK_TIMEOUT_MS??12000)))};
