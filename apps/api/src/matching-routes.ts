import type {FastifyInstance} from "fastify";import {db} from "./db.js";import {requireRoles} from "./rbac.js";import {writeAdminAudit} from "./admin-audit.js";import {analyzeFilteredCandidates,deepSeekConfigured} from "./ai-analysis.js";
export async function matchingRoutes(app:FastifyInstance){
app.get("/v1/admin/vacancies/:code/matches",{preHandler:requireRoles("ADMIN")},async(req:any,reply)=>{const v=await db.query("select * from vacancies where vacancy_code=$1",[req.params.code]);if(!v.rowCount)return reply.code(404).send({error:"VACANCY_NOT_FOUND"});if(!["APROBADA","EN_BUSQUEDA"].includes(v.rows[0].status))return reply.code(409).send({error:"VACANCY_NOT_READY_FOR_MATCHING"});const x=v.rows[0];const q=await db.query(`select candidate_id,candidate_code,full_name,province,district,primary_job_area,skills,valid_until,
(lower(primary_job_area)=lower($1)) role_match,
(lower(work_locations) like '%'||lower($2)||'%') location_match,
coalesce((select array_agg(trim(s)) from unnest(string_to_array(coalesce($3,''),',')) s where trim(s)<>'' and lower(coalesce(skills,'')) like '%'||lower(trim(s))||'%'),'{}'::text[]) skills_overlap,
(case when nullif(trim(coalesce($4,'')),'') is null then null else lower(coalesce(education::text,'')) like '%'||lower(trim($4))||'%' end) education_match,
(case when nullif(trim(coalesce($5,'')),'') is null then null else lower(coalesce(experience::text,'')) like '%'||lower(trim($5))||'%' end) experience_match,
(available_from is null or available_from<=current_date) availability_match,
available_from,availability_notes,
jsonb_build_object('role',lower(primary_job_area)=lower($1),'location',lower(work_locations) like '%'||lower($2)||'%','skills',coalesce((select array_agg(trim(s)) from unnest(string_to_array(coalesce($3,''),',')) s where trim(s)<>'' and lower(coalesce(skills,'')) like '%'||lower(trim(s))||'%'),'{}'::text[]),'education',(case when nullif(trim(coalesce($4,'')),'') is null then null else lower(coalesce(education::text,'')) like '%'||lower(trim($4))||'%' end),'experience',(case when nullif(trim(coalesce($5,'')),'') is null then null else lower(coalesce(experience::text,'')) like '%'||lower(trim($5))||'%' end),'availability',(available_from is null or available_from<=current_date)) match_trace
from candidate_profiles where status='ACTIVO' and valid_until>=current_date order by updated_at desc limit 100`,[x.position,x.work_location,x.skills,x.minimum_education,x.experience_requirement]);return {vacancy:{code:x.vacancy_code,position:x.position},items:q.rows};});
app.post("/v1/admin/vacancies/:code/ai-analysis",{preHandler:requireRoles("ADMIN")},async(req:any,reply)=>{if(!deepSeekConfigured())return reply.code(503).send({error:"AI_NOT_CONFIGURED"});const v=await db.query("select vacancy_code,position,work_location,skills,minimum_education,experience_requirement,schedule,status from vacancies where vacancy_code=$1",[req.params.code]);if(!v.rowCount)return reply.code(404).send({error:"VACANCY_NOT_FOUND"});if(!["APROBADA","EN_BUSQUEDA"].includes(v.rows[0].status))return reply.code(409).send({error:"VACANCY_NOT_READY_FOR_MATCHING"});const x=v.rows[0];const q=await db.query(`select candidate_id,candidate_code,primary_job_area,province,district,work_profile,skills,education,experience,availability_notes,jsonb_build_object('role',lower(primary_job_area)=lower($1),'location',lower(work_locations) like '%'||lower($2)||'%','skills',coalesce((select array_agg(trim(s)) from unnest(string_to_array(coalesce($3,''),',')) s where trim(s)<>'' and lower(coalesce(skills,'')) like '%'||lower(trim(s))||'%'),'{}'::text[]),'education',(case when nullif(trim(coalesce($4,'')),'') is null then null else lower(coalesce(education::text,'')) like '%'||lower(trim($4))||'%' end),'experience',(case when nullif(trim(coalesce($5,'')),'') is null then null else lower(coalesce(experience::text,'')) like '%'||lower(trim($5))||'%' end),'availability',(available_from is null or available_from<=current_date)) match_trace from candidate_profiles where status='ACTIVO' and valid_until>=current_date and (lower(primary_job_area)=lower($1) or lower(work_locations) like '%'||lower($2)||'%' or lower(coalesce(skills,'')) like '%'||lower(coalesce($3,''))||'%') order by updated_at desc limit 40`,[x.position,x.work_location,x.skills,x.minimum_education,x.experience_requirement]);if(!q.rowCount)return {provider:"deepseek",analyses:[]};try{const result=await analyzeFilteredCandidates(x,q.rows,AbortSignal.timeout(12000));await writeAdminAudit({adminUserId:req.authUser!.user_id,action:"AI_CANDIDATE_ANALYSIS",entityType:"VACANCY",entityId:String(req.params.code),metadata:{provider:result.provider,model:result.model,candidates:q.rowCount}});return result;}catch(error){req.log.error(error,"candidate AI analysis failed");return reply.code(502).send({error:"AI_ANALYSIS_FAILED"});}});
app.post("/v1/admin/vacancies/:code/candidates",{preHandler:requireRoles("ADMIN")},async(req:any,reply)=>{const ids=Array.isArray(req.body?.candidate_ids)?req.body.candidate_ids:[];if(!ids.length)return reply.code(400).send({error:"CANDIDATES_REQUIRED"});const uniqueIds=[...new Set(ids.map((id:any)=>String(id).trim()))];const uuid=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;if(uniqueIds.some(id=>!uuid.test(id)))return reply.code(400).send({error:"INVALID_CANDIDATE_ID"});const client=await db.connect();try{await client.query("begin");const v=await client.query("select vacancy_id,package,package_candidate_limit from vacancies where vacancy_code=$1 and status in ('APROBADA','EN_BUSQUEDA') for update",[req.params.code]);if(!v.rowCount){await client.query("rollback");return reply.code(409).send({error:"VACANCY_NOT_READY"});}const limit=v.rows[0].package_candidate_limit;const eligible=await client.query("select candidate_id from candidate_profiles where candidate_id=any($1::uuid[]) and status='ACTIVO' and valid_until>=current_date for update",[uniqueIds]);const eligibleIds=eligible.rows.map((r:any)=>r.candidate_id);let touched=0;for(const id of eligibleIds){const ins=await client.query("insert into vacancy_candidates(vacancy_id,candidate_id,interest_status,contact_authorized,responded_at) values($1,$2,'INTERESADO',true,now()) on conflict(vacancy_id,candidate_id) do update set interest_status='INTERESADO',contact_authorized=true,responded_at=coalesce(vacancy_candidates.responded_at,now()) returning candidate_id",[v.rows[0].vacancy_id,id]);touched+=ins.rowCount??0;}if(!touched){await client.query("rollback");return reply.code(409).send({error:"NO_ACTIVE_CANDIDATES"});}const total=await client.query("select count(*)::int total from vacancy_candidates where vacancy_id=$1",[v.rows[0].vacancy_id]);if(limit!==null&&limit!==undefined&&total.rows[0].total>Number(limit)){await client.query("rollback");return reply.code(400).send({error:"PACKAGE_CANDIDATE_LIMIT",limit:Number(limit),total:total.rows[0].total});}await client.query("update vacancies set status='EN_BUSQUEDA',updated_at=now() where vacancy_id=$1",[v.rows[0].vacancy_id]);await writeAdminAudit({adminUserId:req.authUser!.user_id,action:"CANDIDATES_PRESELECTED",entityType:"VACANCY",entityId:String(req.params.code),metadata:{requested:uniqueIds.length,touched,total:total.rows[0].total}},client);await client.query("commit");return {ok:true,count:touched,total:total.rows[0].total};}catch(e){await client.query("rollback").catch(()=>{});throw e;}finally{client.release();}});
app.get("/v1/candidate/opportunities",{preHandler:requireRoles("CANDIDATO")},async req=>{const q=await db.query(`select dc.delivery_candidate_id,v.vacancy_code,v.position,v.work_location,v.schedule,v.salary,d.sent_at from vacancy_delivery_candidates dc join vacancy_deliveries d on d.delivery_id=dc.delivery_id join vacancies v on v.vacancy_id=d.vacancy_id join candidate_profiles cp on cp.candidate_id=dc.candidate_id where cp.user_id=$1 and d.status='ENVIADA' order by d.sent_at desc`,[req.authUser!.user_id]);return {items:q.rows};});
}
